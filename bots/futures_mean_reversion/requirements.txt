# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8

# Market data — TradingView feed (CME futures, real-time)
tvdatafeed>=2.0.0

# Live broker integrations
requests>=2.31         # Tradovate REST API
websockets>=12.0       # Tradovate WebSocket streaming (optional, for tick feed)

# Optional: QuantConnect LEAN bridge
# lean>=2.0             # install separately: pip install lean

# Optional: Backtrader (alternative backtesting engine)
# backtrader>=1.9.78

# Optional: vectorbt (vectorised backtesting, fast parameter sweeps)
# vectorbt>=0.26

# Dev / notebook
ipython>=8.0
jupyter>=1.0
tabulate>=0.9

# Testing
pytest>=7.4
schedule
backports.zoneinfo;python_version<'3.9'
